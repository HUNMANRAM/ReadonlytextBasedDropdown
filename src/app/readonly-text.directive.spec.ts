import { ReadonlyTextDirective } from './readonly-text.directive';

describe('ReadonlyTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadonlyTextDirective();
    expect(directive).toBeTruthy();
  });
});
